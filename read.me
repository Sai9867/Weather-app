# 🌦️ Weather App

A simple and responsive **Weather Application** built using **HTML, CSS, and JavaScript**. This app allows users to search for any city and get real-time weather information such as temperature, humidity, and weather conditions using a weather API.

---

## 📁 Project Structure
```

weather-app/
├─ index.html
├─ style.css
├─ script.js
└─ assets/
├─ bg.jpg
└─ icons/

```

---

## ✨ Features

- 🌍 Search weather by city name
- 🌡️ Displays temperature in Celsius
- 💧 Shows humidity level
- 🌥️ Weather condition text & icon
- ❌ City not found error handling
- 📱 Fully responsive design
- 🖼️ Background image with blur overlay

---

## 🛠️ Technologies Used

- HTML5
- CSS3
- JavaScript (ES6)
- Weather API (OpenWeatherMap or similar)

---

## 🚀 How to Run the Project

1. Clone or download this repository
2. Open the project folder
3. Open `index.html` in your browser

> 💡 Recommended: Use **VS Code Live Server** for better performance

---

## 🔗 Live Demo

You can host this project using **GitHub Pages**, **Netlify**, or **Vercel**.

Example Live Demo link format:

```

[https://your-username.github.io/weather-app/](https://your-username.github.io/weather-app/)

````

After deployment, paste your live link here.

---

## 🔑 API Setup

1. Create a free account on **OpenWeatherMap**
2. Generate your API key
3. Add the API key inside `script.js`

```javascript
const API_KEY = "YOUR_API_KEY_HERE";
````

---

## ⚠️ API Error Handling

The app handles common API errors gracefully:

* **City not found (404)**

  * Displays a "City Not Found" message with an image

* **Invalid API key (401)**

  * Logs an error in the console

* **Network / Internet error**

  * Shows a friendly message asking the user to check their connection

This ensures a better user experience without app crashes.

---

## 🧩 Key Functionality (JavaScript)

```javascript
fetch(`https://api.openweathermap.org/data/2.5/weather?q=${city}&appid=${API_KEY}&units=metric`)
  .then(response => {
    if (!response.ok) {
      throw new Error("City not found");
    }
    return response.json();
  })
  .then(data => console.log(data))
  .catch(error => console.error(error));
```

---

## 🌤️ Weather Icon Usage

Each weather condition uses a specific SVG icon from the `assets/weather` folder:

| Weather Condition              | Icon File          |
| ------------------------------ | ------------------ |
| Clear sky                      | `clear.svg`        |
| Clouds                         | `clouds.svg`       |
| Rain                           | `rain.svg`         |
| Drizzle                        | `drizzle.svg`      |
| Thunderstorm                   | `thunderstorm.svg` |
| Snow                           | `snow.svg`         |
| Atmosphere (mist, smoke, haze) | `atmosphere.svg`   |

Icons are selected dynamically based on the weather condition returned by the API.

---

## 🖼️ Screenshots

Add screenshots of your Weather App UI here so visitors can quickly understand the app.

Suggested screenshots:

* 🔍 Search city screen
* 🌤️ Weather result screen
* ❌ City not found screen

Example:

```
/screenshots/home.png
/screenshots/weather-result.png
/screenshots/not-found.png
```

---

## ❗ Common Issues & Fixes

* **Weather not loading?**

  * Check your API key
  * Ensure internet connection is active

* **City not found error?**

  * Enter correct city name

---

## 📌 Future Enhancements

* 📍 Current location weather (GPS)
* 🌙 Dark / Light mode
* 📆 7-day weather forecast
* 🌬️ Wind speed & pressure

---

## 👨‍💻 Author

*Sairam Adepu**
Frontend Developer

---

⭐ If you like this project, don’t forget to star the repository!
