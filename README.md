# Weather-app
A responsive weather application built using HTML, CSS, and JavaScript that fetches real-time weather data from a public API. Users can search by city name to view temperature, humidity, and weather conditions with dynamic icons, a clean UI, and proper error handling for invalid inputs and network issues.
